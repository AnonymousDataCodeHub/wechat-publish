---
name: wechat-publish
description: 用户想把文章发布到微信公众号时使用。覆盖完整流程：把原始 markdown 转成公众号友好的排版 markdown，再转成 HTML，最后通过微信 API 自动上传图片并推送到草稿箱。触发短语包括「微信公众号排版」「发布到公众号」「转成公众号格式」「推送到草稿箱」「微信文章排版」，或当用户在桌面操作 `article*.md` / `article_wechat.*` 这类文件时。
---

# 微信公众号发布流水线

经过多轮迭代验证过的端到端流程。产出克制、单色蓝家族的设计风格，在微信移动端显示效果良好。**三个阶段必须分开走，不能跳阶段、不能合并。**

## 阶段 1 — 原始 markdown → 排版 markdown

**输入**：用户的原始 markdown 文章。
**输出**：同目录下加 `_wechat.md` 后缀，例如 `article.md` → `article_wechat.md`。

> **命名约定**：默认用下划线 `_wechat.md` / `_wechat.html`。`-wechat.md`（连字符）也能跑通脚本（`--html` 接受任意文件名），但产物对外保持下划线一致性。

**铁律（这些是用户反复反馈出来的，不要放松）：**

1. **文本不可改动。** 原文的字、标点、甚至错别字（`propability`、`Few-short`、`Zero-short` 这些原文就有的笔误都要保留）必须逐字保留。用户原话：*"我怕你改出知识性错误"*。只有纯视觉用途的分隔标点可以转为设计（例如 `1）` 转为数字徽章，加粗标签后面的 `：` 转为卡片标题的视觉分隔），但任何有语义的字都不能动。

2. **图片位置不可改动。** 每一张 `![image](...)` 必须留在原文的位置，穿插在它前后段落之间。**绝对不能**把图片收集起来在章节末尾统一展示。Transformer「关键创新」是典型例子：每条创新点后面紧跟自己的配图。

3. **只用 3 种颜色，且必须来自同一色系（同色家族 + 不同明度）。** 不要彩虹、不要多色拼接做强调色。

   **默认色板（蓝家族）——用于绝大多数科技/分析/教程类内容：**
   - `#2C5282` — 主色，中深蓝（标题、强调、链接）
   - `#1E3A5F` — 略深蓝（**只**用作渐变第二色，和 `#2C5282` 搭配）
   - `#F0F4F8` / `#F5F8FB` / `#EAF0F7` — 浅蓝灰底色（卡片背景，同色系不同明度）

   **允许切换的备选家族——只在内容本身有强烈文化/历史/情绪色彩时（如明清史、佛学禅意、新年节庆）：**
   - 深红家族：主色 `#8B0000`、渐变深 `#5C0000`、底色 `#FAF7F2` —— 适合明清/历史厚重题材
   - 墨黑家族：主色 `#2C2C2C`、辅 `#555`、底色 `#F5F5F5` —— 适合极简/古风/书法题材
   - 青绿家族：主色 `#2D5F4F`、渐变深 `#1F4536`、底色 `#F0F5F2` —— 适合自然/茶道/禅意题材

   **切换前要先和用户确认**，并且仍然遵守"3 色 + 同色系明度差"的克制感。**禁止**回到 `#4D6BFE` + `#6B47DC` + `#FF6B35` + `#FAAD14` + `#FF4D4F` 这种多色拼接（用户明确拒绝过）。

   - 文字色不变：`#333` 正文 / `#555` 次级（这两个算"文字"不算"色"）。

4. **不要用斜体做强调。** 原文里 `_1）xxx：_` 这种斜体编号项被用户明确说丑。一律转成**编号卡片**（见 `snippets.md`）。

5. **打破单调感。** 一段段平铺的 `<p>` 是"太单调"的。用片段库交错使用：
   - `1) 2) 3)` 编号子项 → **带 `01/02/03` Georgia 衬线数字徽章的卡片**
   - `a. b. c.` 子标签 → **4×18px 竖条 + 深蓝粗体**
   - 定义类标签（`**定义：**`、`**自回归语言模型：**`）→ **左竖条浅蓝卡内的标签徽章**（**只**在开篇 LLMs 一节用，定调子）
   - 关键结论句 → **浅蓝底 + 左竖条 callout**
   - 大节标题（一/二/三）→ **右侧切圆角的渐变胶囊**
   - 封面和结尾 → **深蓝渐变卡 + 英文 caption + 细分隔线**

6. **层级靠字号字重区分，不靠颜色。** H3 用 `#2C5282` 17px 600 字重 + 底边框。H4 用 `#2C5282` 15px 600 字重 + 左竖条。两个都是蓝色，不要换色。

每个片段的完整 HTML 见本目录下的 **`snippets.md`**。**不要自由发挥，照抄验证过的片段。**

## 阶段 2 — 排版 markdown → HTML

**输入**：阶段 1 产出的 `*_wechat.md`。
**输出**：同目录的 `*_wechat.html`。

排版 markdown 本身已经是大量 inline-styled `<section>` / `<p>`，转换只需要：

1. 包一层完整 HTML 文档，外层 720px 居中容器。固定用这段 body 包装：
   ```html
   <div style="max-width: 720px; margin: 0 auto; padding: 24px 18px; font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif; color: #333; background: #FFFFFF; box-sizing: border-box;">
   ```
2. 每个 `![image](url)` 转成：
   ```html
   <img src="url" alt="image" style="max-width: 100%; display: block; margin: 14px auto; border-radius: 4px;" />
   ```
3. 已有的 `<img width="...">` 标签也统一规范成 `max-width: 100%` 自闭合形式（移除 `width="1219"` 这类固定宽度，移动端会溢出）。
4. 每个 `<a>` 加 `text-decoration: none`，去掉默认下划线。
5. 长 URL 在 `<a>` 上加 `word-break: break-all`，避免溢出。
6. 文本节点（非标签）里的 `>` 转义为 `&gt;`，`<` 转义为 `&lt;`（比如 `PPO=>GRPO` → `PPO=&gt;GRPO`）。
7. 所有 inline 样式原样保留，**不要**抽到 `<style>` 块。后面 publish 脚本会跑 `premailer` 兜底再 inline 一次，但起步就 inline 才不会丢。

## 阶段 3 — 发布：上传图片 + 推送草稿

**脚本**：本目录的 `publish.py`。

**一次性配置** — 见 `config.md`，用户绑定公众号的 AppID 和 AppSecret。

### 推送前的版权检查（必做）

如果文章内容是**第三方版权作品**（小说、书籍、论文、他人公众号文章、影视剧台词等），推送前必须和用户确认以下三件事：

1. **不要勾选「原创」**——勾了等于双重违规（侵权 + 虚假声明）。
2. **不要开赞赏 / 不要带广告 / 不要付费阅读**——一旦商业化使用，侵权性质从「不当」升级到「严重」。
3. **文末加版权声明**——降低恶意度（**不能**让侵权变合法，但能减少投诉概率）。声明模板：
   ```
   ———— 版权声明 ————
   本文节选自 <作者>《<书名/原作>》（<出版方>）
   原著版权归作者及出版方所有，本文仅作个人阅读分享，无任何商业用途
   如涉版权问题，请联系作者删除
   ```

判断标准：如果文章里有连续 200 字以上的他人原文逐字摘录，就触发以上检查。如果不确定，问用户："这段内容是你原创还是引用？引用的话出处是？"

**如果用户坚持不加声明、不取消「原创」**，告知风险后由用户决定，但要在推送日志里记录这次跳过。

### 运行

脚本路径取决于本 skill 安装在哪里。常见位置：
- 全局 skill 目录：`~/.claude/skills/wechat-publish/publish.py`
- 项目内 skill 目录：`<repo>/.claude/skills/wechat-publish/publish.py`
- 本地存放：`~/Desktop/wechat-publish/publish.py`

调用前先 `ls` 确认实际路径，或者用 `find ~ -maxdepth 4 -name publish.py -path "*wechat-publish*"` 定位。

```bash
cd <html-所在目录>
python <publish.py 的实际路径> --html article_wechat.html --title "<文章标题>"
```

> **首次运行前**：先装依赖 `pip install requests beautifulsoup4 premailer`（详见 `config.md`）。脚本不会自动装，缺包会直接 `ModuleNotFoundError`。

脚本会：
1. 调微信 API 拿 `access_token`。
2. 下载每个 `<img src="...">` 的 URL，第一张图上传两次（一次永久素材作封面 `thumb_media_id`、一次图文图片用于正文），其余只上传一次。替换 HTML 里的链接。
3. 跑 `premailer.transform()` 把剩余 CSS 内联（兜底防御，微信会剥掉 `<style>` 和 `<head>`）。
4. POST 到 `/cgi-bin/draft/add`，返回草稿 `media_id`。用户去公众号后台 → 草稿箱审阅发布。

**用户验证过的关键行为：**
- 第一张图自动作为封面（封面图）。写作时要意识到这点。
- **图片支持本地路径**——`<img src="image.png">` 这种相对路径会基于 HTML 文件所在目录解析后直接读取上传，不必先传图床（2026-06 验证修复）。HTTP 链接也照常工作。
- 每张图上传之间 sleep 0.3 秒，避免限流。
- JSON payload 必须 `ensure_ascii=False`，否则中文在草稿里会乱码。
- 常见错误码：`40001`（凭证错）、`48001`（公众号未开通草稿箱接口），脚本会给清晰提示。

## 被调用时的工作流

1. 问用户哪个文件是源文件（默认在当前目录找 `article*.md`，多个就让用户选）。
2. **阶段 1**：读源文件，产出 `*_wechat.md`。简短总结哪些片段类型应用在哪里（**不要**贴完整 diff）。
3. 停下让用户审阅。用户可能想改文字、换图、调整哪些段做 callout。迭代到用户认可。
4. **阶段 2**：基于用户批准的 md 生成 `*_wechat.html`。用户想看效果就用浏览器打开。
5. 再停一次。用户可能预览后还要微调。
6. **阶段 3**：**只有**用户明确说「推送」「发布」「上传」之类才执行。确认 AppID 已配置，运行 `publish.py`，上报草稿 `media_id`，告诉用户去公众号后台 → 草稿箱看。

**没有用户明确放行不要执行阶段 3** — 推送草稿是公众号管理员可见的、且上传到第三方服务，按 `git push` 的标准对待。

## 本 skill 的文件

- `SKILL.md` — 当前文件
- `snippets.md` — 每种排版片段的可复制 HTML，附使用场景说明
- `publish.py` — 上传 + 推草稿脚本
- `config.md` — AppID/AppSecret 配置方法（推荐用环境变量，不要硬编码）